package com.faacets
package operation
package relabeling
/*
import org.scalatest.FunSuite

import spire.syntax.all._
import spire.math.Rational

import qalg.immutable.QVector

import data._
import core.perm._
import net.alasc.syntax.all._

class ParserSuite extends FunSuite {
  test("Identity are parsed correctly") {
    assert("".fromText[Relabeling] == Relabeling.Algebra.id)
    assert("".fromText[PartyRelabeling] == PartyRelabeling.Algebra.id)
  }
}
*/