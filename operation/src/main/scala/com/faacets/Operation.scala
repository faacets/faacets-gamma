package com.faacets
/*
import spire.math.{Interval, Bounded, All, Empty, Point, Above, Below, Rational}
import spire.syntax.group._

import core._
import data._

import operation._
import operation.syntax.all._

object Operation {
  def symmetricRepresentative(expr: Expr): Expr =
    expr.symmetricRepresentative.get
  def cyclicRepresentative(expr: Expr): Expr =
    expr.cyclicRepresentative.get
}
*/